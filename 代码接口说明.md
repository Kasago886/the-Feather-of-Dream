# 代码说明

# 目录
- [Consts](#consts)
- [InputListener](#inputlistener)
- [AnimationTools](#animationtools)
    - [AnimatorBoolManager](#animatorboolmanager)
        - [SetTrue(string argument)](#settruestring-argument)
        - [SetFalse(string argument)](#setfalsestring-argument)
- [Archive](#archive)
    - [Archive](#archive)
    - [ArchiveManager](#archivemanager)
        - [currentArchive](#currentarchive)
        - [ReadArchive(int archiveIndex)](#readarchiveint-archiveindex)
        - [SaveArchive(Archive archive, int archiveIndex)](#savearchivearchive-archive-int-archiveindex)
        - [GetArchive(int archiveIndex)](#getarchiveint-archiveindex)
        - [DeleteArchive(int archiveIndex)](#deletearchiveint-archiveindex)
    - [ArchiveDebugWindow](#archivedebugwindow)
- [Controller](#controller)
    - [PlayerController](#playercontroller)
    - [ControllerStates](#controllerstates)
        - [ControllerState](#controllerstate)
        - [ControllerxxxState](#controllerxxxstate)
- [UIs](#uis)
    - [Dialog](#dialog)
        - [UnityEvent endEvent](#unityevent-endevent)
        - [Read(string TextFile)](#readstring-textfile)
        - [Clear()](#clear)
    - [EquipmentPanel](#equipmentpanel)
        - [Item](#item)
- [InteractiveObjects](#interactiveobjects)
   - [InteractiveObject](#interactiveobject)
      - [PlayerInteract](#playerinteract)  


## Consts
- 存储常量，如各种图层，标签名称

## InputListener
- 监听鼠标点击和按键输入，提供事件接口
    可在此处添加自定义事件，如：按下某个键触发某个事件
    例：
    ```csharp
    // 自定义事件
    public UnityEvent SpaceEvent;

    void Update(){
        // 监听按键（空格键）
        if (Input.GetKeyDown(KeyCode.Space)){
            // 触发自定义事件
            SpaceEvent?.Invoke();
        }
    }
    ```
    在InputListener物体中可绑定自定义事件

---

## AnimationTools
### AnimatorBoolManager
- 动画布尔值管理器，提供动画布尔值的设置功能
#### SetTrue(string argument)
用于设置动画布尔值为True

#### SetFalse(string argument)
用于设置动画布尔值为False

#### SwitchValue(string argument)
用于切换动画布尔值，True->False->True

---

## Archive
### Archive
- 存档的数据结构

### ArchiveManager
- 存档管理，提供存档读取、存档保存、存档删除功能
    存档以json格式保存，名称为存档编号

#### Archive currentArchive
当前存档

#### ReadArchive(int archiveIndex)
用于设置currentArchive

#### SaveArchive(Archive archive, int archiveIndex)
用于保存存档

#### GetArchive(int archiveIndex)
用于读取存档，返回Archive对象

#### DeleteArchive(int archiveIndex)
用于删除存档

### ArchiveDebugWindow
- 存档调试窗口，用于查看存档信息
    在Tool菜单中可打开

---

## Controller
### PlayerController
- 玩家控制器，提供移动、跳跃、攻击、冲刺等功能
    Onxxx()方法为执行相应功能的事件，如OnJump()
    Statexxx()方法为执行当前状态下的功能，如StateJump()就是执行当前状态的OnJump()
    
    <br/>
    例如：Sprinting状态下不能跳跃，执行StateJump()时无效果，而OnJump()方法可以触发跳跃效果。

### ControllerStates
#### ControllerState
- 玩家状态机的基类，包含Onxxx()方法，回调到PlayerController中执行相应功能

#### ControllerxxxState
- 玩家状态机的具体状态，继承自ControllerState，通过复写Onxxx()方法实现状态的不同功能或者禁用功能

---

## UIs
### Dialog
- 对话框，提供显示对话内容和头像等功能
#### UnityEvent endEvent
对话结束时触发的事件，在DialogPanel物体上绑定

#### Read(string TextFile)
读取对话内容，并显示在对话框中

#### Clear()
清空对话框内容

### EquipmentPanel
#### Item
- 装备栏中的物品数据结构
---
## InteractiveObjects
### InteractiveObject 
==本类用于继承，继承的脚本需要覆写Interactive()==

置于可交互物品上，会自动添加Polygon Collier2D 
使用方法：
选择：鼠标触发或者按键触发（==必须2选1==）
#### 鼠标触发
无需其他操作，游戏过程中用鼠标与物体交互便可以调动Interact()
#### 按键触发
##### mode1
需要填写最大触发距离，如果需要进行进一步的限制，选择morelimit，可以进一步在
x轴与y轴上进行限制（注意：2者只能填一个）此时，你可以通过查看scene中的所呈
现的范围，来进行进一步的限制
##### mode2
需要给可交互的物体添加rigidbody2D脚本，并将polygon collider2D中的trigger勾选，
并给玩家添加一个PlayerInteract脚本
##### 触发按键名称
输入按键名称即可（==注意：必须小写==）
### PlayerInteract
- 无他，只需将其置于玩家上尔
  


    