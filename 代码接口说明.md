# 代码说明

# 目录
- [Consts.cs](#consts)
- [InputListener.cs](#inputlistener)
- [PlayerController.cs](#playercontroller)
    - [ControllerState.cs](#controllerstate)
    - [ControllerxxxState.cs](#controllerxxxstate)
- [ArchiveManager.cs](#archivemanager)
    - [Archive.cs](#archive)
- [Dialog.cs](#dialog)
    - [Read(string TextFile)](#readstring-textfile)
    - [Clear()](#clear)
    - [UnityEvent endEvent](#unityevent-endevent)

## Consts.cs
- 存储常量，如各种图层，标签名称

## InputListener.cs
- 监听鼠标点击和按键输入，提供事件接口
    可在此处添加自定义事件，如：按下某个键触发某个事件
    例：
    ```csharp
    // 自定义事件
    public UnityEvent SpaceEvent;

    void Update(){
        // 监听按键（空格键）
        if (Input.GetKeyDown(KeyCode.Space)){
            // 触发自定义事件
            SpaceEvent?.Invoke();
        }
    }
    ```
    在InputListener物体中可绑定自定义事件

## PlayerController.cs
- 玩家控制器，提供移动、跳跃、攻击、冲刺等功能
    Onxxx()方法为执行相应功能的事件，如OnJump()
    Statexxx()方法为执行当前状态下的功能，如StateJump()就是执行当前状态的OnJump()
    
    <br/>
    例如：Sprinting状态下不能跳跃，执行StateJump()时无效果，而OnJump()方法可以触发跳跃效果。

### ControllerState.cs
- 玩家状态机的基类，包含Onxxx()方法，回调到PlayerController.cs中执行相应功能

### ControllerxxxState.cs
- 玩家状态机的具体状态，继承自ControllerState.cs，通过复写Onxxx()方法实现状态的不同功能或者禁用功能

## ArchiveManager.cs
- 存档管理，提供存档读取、存档保存、存档删除功能
    存档以json格式保存，名称为存档编号

### Archive.cs
- 存档的数据结构

## Dialog.cs
- 对话框，提供显示对话内容和头像等功能
    ### Read(string TextFile)
    - 读取对话内容，并显示在对话框中

    ### Clear()
    - 清空对话框内容

    ### UnityEvent endEvent
    - 对话结束时触发的事件，在DialogPanel物体上绑定
    